---
cert-manager:
  crds:
    enabled: true
  global:
    leaderElection:
      namespace: cert-manager
  config:
    enableGatewayAPI: true

acme:
  email: invalid@labs.local

certificates:
  dnsNames:
  - labs.local