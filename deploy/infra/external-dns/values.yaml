external-dns:
  env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-api-key
        key: apiKey

  domainFilters:
  - labs.local
  policy: sync
  provider:
    name: cloudflare
  sources:
    - ingress
    - gateway-httproute
    - gateway-grpcroute
    - gateway-tlsroute
    - gateway-tcproute
    - gateway-udproute

cloudflare:
  apiToken: mytoken
